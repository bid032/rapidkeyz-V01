# RapidKeyz Implementation Guide

هذا الملف يشرح بالتفصيل طريقة تنفيذ كل تعديل من التعديلات المطلوبة، مع توضيح الملفات التي سيتم تعديلها والخطوات اللازمة لكل تعديل.

---

## 1. تأكيد الحذف في الداشبورد
**الملفات المتأثرة:**
- `src/components/AdminNotifications.tsx`
- `src/routes/stock.tsx` (لصفحة المخزون)
- `src/routes/checkout.tsx` (لصفحة الطلبات)
- `src/server.ts` (للمنطق الخلفي)
- `supabase/migrations/` (لإضافة حقل الباسورد)

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة حقل `delete_password` في جدول الإعدادات
2. تعديل مكون `AdminNotifications` لإضافة نافذة تأكيد الحذف
3. إضافة زر "مسح كل الطلبات" يظهر فقط للادمن
4. إنشاء نافذة لإدخال الباسورد قبل الحذف
5. تعديل منطق الحذف في الخادم للتحقق من الباسورد

---

## 2. إعدادات الدفع التجريبي
**الملفات المتأثرة:**
- `src/routes/settings.tsx` (أو إنشاء صفحة جديدة)
- `src/server.ts`
- `supabase/migrations/` (لإضافة حقل الإعدادات)

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة حقل `test_payment_enabled` في جدول الإعدادات
2. إنشاء مكون جديد في صفحة الإعدادات لتفعيل/تعطيل الدفع التجريبي
3. تعديل منطق الدفع في الخادم للتحقق من حالة الإعداد

---

## 3. إعدادات رقم الانستاباي/المحفظة للدفع اليدوي
**الملفات المتأثرة:**
- `src/routes/settings.tsx`
- `supabase/migrations/`

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة حقول:
   - `instapay_number`
   - `wallet_number`
2. إضافة حقول الإدخال في صفحة الإعدادات
3. تعديل منطق الدفع اليدوي لاستخدام هذه الأرقام

---

## 4. أيقونة التحميل مع الصور للخدمات
**الملفات المتأثرة:**
- `src/components/ProductCard.tsx`
- `src/components/HeroCanvas.tsx`
- `src/components/BrandsStrip.tsx`
- `supabase/migrations/` (لإضافة حقل الأيقونة)
- `src/server.ts` (لجلب البيانات)

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة حقل `icon_url` في جدول المنتجات
2. تعديل مكون `ProductCard` لعرض الأيقونة في الهيرو وشريط الشركاء
3. تعديل مكون `HeroCanvas` لعرض الأيقونات المحددة
4. تعديل مكون `BrandsStrip` لعرض الأيقونات بدلاً من الصور في الشريط

---

## 5. أيقونات الهيرو القابلة للتخصيص
**الملفات المتأثرة:**
- `src/routes/settings.tsx`
- `src/components/HeroCanvas.tsx`
- `supabase/migrations/`

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة جدول `hero_icons` يحتوي على:
   - `icon_id`
   - `icon_url`
   - `is_active`
2. إنشاء واجهة في الإعدادات لتحديد الأيقونات النشطة
3. تعديل مكون `HeroCanvas` لعرض الأيقونات المحددة فقط

---

## 6. تفاصيل الخطة الكاملة في مخزون التسليم الفوري
**الملفات المتأثرة:**
- `src/routes/stock.tsx`
- `supabase/migrations/` (لتعديل جدول المنتجات)
- `src/server.ts` (لجلب البيانات المعدلة)

**خطوات التنفيذ:**
1. تعديل هجرة قاعدة البيانات لإضافة حقول:
   - `plan_duration`
   - `plan_type`
   - `account_type`
2. تعديل مكون `StockDispenser` لعرض التفاصيل الكاملة
3. تعديل منطق جلب البيانات لتشمل الحقول الجديدة
4. تعديل ربط شيت اكسل ليتوافق مع الحقول الجديدة

---

## 7. صلاحيات كاملة لحسابات الأدمن
**الملفات المتأثرة:**
- `src/server.ts` (لتعديل منطق الصلاحيات)
- `src/components/AdminNotifications.tsx` (لإزالة القيود)

**خطوات التنفيذ:**
1. تعديل منطق التحقق من الصلاحيات لمنح الأدمن كل الصلاحيات
2. إزالة جميع القيود عن حسابات الأدمن في الواجهات المختلفة
3. تعديل مكون `AdminNotifications` لإظهار جميع الإشعارات بدون قيود

---

## 8. مؤشرات التحميل في الموقع
**الملفات المتأثرة:**
- `src/components/ProductCard.tsx` (لتسليم الطلبات)
- `src/components/AdminNotifications.tsx` (لعمليات الحذف)
- `src/components/ToastNotifications.tsx` (لإشعارات النجاح)

**خطوات التنفيذ:**
1. إنشاء مكون جديد `LoadingIndicator` لإعادة الاستخدام
2. إضافة مؤشر التحميل لعمليات تسليم الطلبات
3. إضافة مؤشر التحميل لعمليات الحذف
4. إضافة إشعارات النجاح بعد الانتهاء

---

## 9. سلايدر لإشعارات الطلبات
**الملفات المتأثرة:**
- `src/components/AdminNotifications.tsx`

**خطوات التنفيذ:**
1. تعديل مكون الإشعارات لإظهار سلايدر عند تجاوز 10 طلبات
2. إضافة مكتبة سلايدر مناسبة (مثل embla-carousel)
3. تعديل التصميم ليتناسب مع السلايدر

---

## 10. توحيد بيانات الأسعار في الداشبورد
**الملفات المتأثرة:**
- `src/components/ProductCard.tsx`
- `src/routes/product.$slug.tsx`
- `src/server.ts`

**خطوات التنفيذ:**
1. مراجعة منطق جلب الأسعار في الخادم
2. تعديل مكون `ProductCard` لعرض السعر قبل الخصم دائماً
3. تعديل صفحة تفاصيل المنتج لعرض السعر قبل الخصم
4. التأكد من تطابق البيانات بين جميع الصفحات

---

## 11. منع تكرار عملية التسليم
**الملفات المتأثرة:**
- `src/routes/stock.tsx`
- `src/server.ts`

**خطوات التنفيذ:**
1. إضافة منطق في الخادم لمنع تكرار التسليم لنفس الطلب
2. إضافة مؤشر تحميل أثناء عملية التسليم
3. إضافة رسالة تأكيد عند الانتهاء
4. تعديل مكون `StockDispenser` لمنع تكرار الضغط على الزرار

---

## 12. تعديل وصف اللوجو في الفوتر
**الملفات المتأثرة:**
- `src/components/Footer.tsx`
- `src/routes/settings.tsx`
- `supabase/migrations/`

**خطوات التنفيذ:**
1. إنشاء هجرة لقاعدة البيانات لإضافة حقل `footer_logo_description`
2. إضافة حقل في صفحة الإعدادات لتعديل الوصف
3. تعديل مكون `Footer` لعرض الوصف من قاعدة البيانات

---

## 13. إزالة وصف الخدمة من كروت الهوم بيدج
**الملفات المتأثرة:**
- `src/components/ProductCard.tsx`

**خطوات التنفيذ:**
1. تعديل مكون `ProductCard` لإزالة وصف الخدمة من الكروت
2. الاحتفاظ بباقي المعلومات (الاسم، السعر، الصورة)

---

## 14. مركزية إشعارات الموقع
**الملفات المتأثرة:**
- `src/components/ToastNotifications.tsx` (أو إنشاء مكون جديد)
- جميع الملفات التي تستخدم الإشعارات

**خطوات التنفيذ:**
1. إنشاء مكون جديد `CenteredNotification` للإشعارات المركزية
2. تعديل جميع الإشعارات لاستخدام المكون الجديد
3. إضافة منطق لتحديد منتصف الشاشة
4. ضمان ظهور الإشعار بالكامل في حدود الشاشة

---

## ملاحظات عامة للتنفيذ:
1. **التأكد من عدم تغيير الوظائف الحالية**: يجب أن تبقى جميع الوظائف الحالية كما هي، مع إضافة التعديلات الجديدة فقط.
2. **الاختبار المستمر**: يجب اختبار كل تعديل فور تنفيذه للتأكد من عدم تأثيره على باقي الوظائف.
3. **التوثيق**: يجب تحديث ملف CHANGELOG.md بعد تنفيذ كل تعديل.
4. **النسخ الاحتياطي**: يوصى بعمل نسخة احتياطية من قاعدة البيانات قبل إجراء أي هجرات.
5. **التنسيق**: يجب الالتزام بتنسيق الكود الحالي للمشروع.